let countries = [];

// Lo  que hace el for of es:
// personas = countries[i]
for (let personas of countries)
{

}

//Insertar codigo de JavaScript 


//Métodos de Cadenas
concat() // junta dos o mas cadenas y retorna una nueva
startWith() // si una cadena comienza con los caracteres de otra cadena, devuelve true, sino devuelve false.
endsWith() // si una cadena termina con los caracteres de otra cadena, devuelve true, sino devuelve false.
includes() // si una cadena puede encontrarse dentro de otra cadena, devuelve true, sino devuelve false.
indexof() // devuelve el indice del primer caracter de la cadena, si no existe, devuelve -1
lastIndexof() // devuelve el ultimo indice del primer caracter de la cadena, si no existe, devuelve -1
padStart() //[propuesta de Estandar] Rellenar cadena al principio de con los caracteres deseados.
padEne() //[propuesta de ECMA]  Rellenar cadena al final con los caracteres deseados
repeat() // devuelve la misma cadena pero repetida la cantidad que le indiquemos

split() // divide la cadena como le pidamos
substring() // nos retorna un pedazo de la cadena que seleccionemos
toLowerCase() // Convierte una cadena a minuscula
toUpperCase() // convierte una cadena a mayuscula
tostring() // método devuelve una cadena, que representada al objeto en especifico 
trim() // elimina los espacios en blanco al principio y final de la cadena.
trimEnd() //
trimStart() //
valueOf() // retorna el valor primitivo de un objeto string


//Métodos de Arrays
//	________Transformadores________
pop() // elimina el ultimo elemento de un array y lo devuelve
shift() // elimina el primer elemento de un array y lo devuelve
push() // agrega un elemento al array al final de la lista
reverse() // invierte el orden de los elementos de un array
unshift() // agrega uno o mas elementos al inicio del array, y devuelve la nueva logitud del array
sort() // ordena los elementos de un arreglo localmente y devuelve el arreglo ordena
splice() // cambia el contenido de un array eliminando elementos existentes y/o agregando nuevos elementos

//	________Accesores________ 
join() // une todos los elementos de una matriz en una cadena y lo devuelve.
slice() // devuelve una parte del array dentro un nuevo array empezando por inicio hasta el fin (fin no incluido)
